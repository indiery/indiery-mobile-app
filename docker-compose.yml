services:
  mongo:
    image: mongo:7
    container_name: indiery-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - indiery_mongo_data:/data/db

volumes:
  indiery_mongo_data:
